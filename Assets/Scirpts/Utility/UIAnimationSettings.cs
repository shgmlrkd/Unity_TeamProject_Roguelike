public static class UIAnimationSettings
{
    // UI 애니메이션 시간

    // =========================
    // 버튼 애니메이션
    // =========================

    // 마우스 오버 시 확대 배율
    public const float HoverScale = 1.1f;

    // 클릭 시 튕기는 강도
    public const float PunchPower = 0.1f;


    // =========================
    // 애니메이션 실행 시간
    // =========================

    // 빠른 애니메이션
    public const float FastDuration = 0.15f;

    // 일반 애니메이션
    public const float NormalDuration = 0.2f;

    // 느린 애니메이션
    public const float SlowDuration = 0.5f;

    // 페이드 인/아웃 애니메이션
    public const float FadeDuration = 1.0f;


    // =========================
    // 시퀀스 설정
    // =========================

    // 애니메이션 진행 중 콜백을 호출할 비율
    public const float CallbackRatio = 0.25f;
}